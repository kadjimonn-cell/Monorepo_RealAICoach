# Production Deploy Failure RCA (PullSource / GCS template) — Checkpoints A–D (2026-07-16)

Task: analyze production deployment build error logs, debug, and fix (code-level only per user).

## Checkpoint A — Baseline evidence
- Deploy fails at FIRST stage (PullSource / prepare build context), identically across BUILD/DEPLOY/HEALTH_CHECK/MANAGE_SECRETS/MONGODB_MIGRATE:
  `dynamic_manifest enabled but failed to load templates from GCS: failed to read Dockerfile from GCS: failed to create GCS reader: storage: object doesn't exist`
- App state at time of failure: deployment_agent static scan PASS (zero findings, twice), `yarn build` EXIT:0 proven locally, preview fully working (health 200, login E2E 10/10).
- /app/.emergent/emergent.yml (platform-generated, mtime 06:38 = deploy attempt time): env_image_name "expo_mongo_base_image_cloud_arm:release-11022026-1".

## Checkpoint B — Root cause (troubleshoot_agent RCA, 10/10 steps)
- The error occurs BEFORE any app code is evaluated: the deployer cannot fetch the Dockerfile template for base image tag `expo_mongo_base_image_cloud_arm:release-11022026-1` from the platform's GCS bucket (object missing platform-side).
- Base image selection is entirely server-side; the repo has no Dockerfile/manifest that controls it; emergent.yml is regenerated by the platform at deploy time.
- Conclusion: PLATFORM-INFRASTRUCTURE issue. No code-level change in /app can fix it. deployment_agent found zero code issues because there are none.

## Checkpoint C — Implementation
- No code changes made (none applicable — making speculative changes would be a fake fix).
- support_agent engaged; guidance: contact support@emergent.sh with job ID 22957321-afe5-4e3a-a232-116e4e47efa6, the exact error, and the base image tag; platform team must publish/repair the GCS template or fix tag selection. Retrying deploy may help only after platform fix.

## Checkpoint D — Final evidence
- App remains healthy in preview: backend/expo/mongodb RUNNING, /api/health 200, preview 200 (verified this session).
- Deployment blocked solely by missing platform GCS template; resolution owner: Emergent platform team.
